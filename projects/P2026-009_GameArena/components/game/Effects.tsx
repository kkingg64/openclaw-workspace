'use client';

// Post-processing effects (bloom, vignette)
// Using drei's built-in effects for simplicity

export function GameEffects() {
  // Effects are handled via CSS vignette for HP-critical state
  // and Three.js built-in bloom via emissive materials
  // Full post-processing pipeline can be added via @react-three/postprocessing
  return null;
}
