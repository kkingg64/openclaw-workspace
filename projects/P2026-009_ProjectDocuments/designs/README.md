# designs/ — Visual Exports + UAT Evidence

> ⚠️ MADHORSE 主要設計工具：**Pencil CLI**（自動化設計 + PNG export）
> Prototype/互動流程：**Penpot**（瀏覽器 canvas 即時可見）
> 即 Figma 已废棄，唔好再用。

Store these files here:

- `{PROJECT_ID}_{ScreenName}_{Breakpoint}.png` — Pencil CLI export_nodes 輸出
- `{PROJECT_ID}_{ScreenName}.pen` — Pencil 設計原始檔
- `uat_screenshots/TC-XXX.png` — Phase 5 UAT browser 截圖

Subfolders:

```
designs/
├── exports/          ← Pencil CLI PNG 交付物
└── uat_screenshots/  ← Phase 5 UAT browser 截圖
```

Do not keep this folder empty for design-heavy projects.