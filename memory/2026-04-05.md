# Memory — 2026-04-05

## MADHORSE HQ (P2026-008) — Phase 6

### Dashboard Deployment Architecture (CRITICAL)
- **Docker build context issue:** `/opt/dashboard_build/` is the build context, NOT the workspace. Workspace is at `/root/.openclaw/workspace/projects/p2026-008-madhorse/`
- **Workflow:** Edit workspace files → rsync to `/opt/dashboard_build/` → `docker build` in `/opt/dashboard_build/`
- **Critical fix:** After every rebuild, sync static files: `docker exec dashboard tar -C /app/.next -c static | tar -C /opt/dashboard_build/.next -x`

### Dashboard v13 + Trends Microserver
- Dashboard container: `madhorse-dashboard:v13` on port 3002
- **Trends microserver:** Standalone Node.js on port 3003 (`/tmp/trends_server.js`) — bypasses broken Next.js build
- Nginx proxy: `/api/research/trends` → `http://localhost:3003`
- **MEMORY_RESEARCH_PATH:** Must be `/app/memory/research` (not `/opt/dashboard_build/...`)

### Sidebar Nav Fix
- File: `app/(dashboard)/Sidebar.tsx` + `MobileNav.tsx`
- `navItems` array needed: Dashboard, Projects, Agents, Research, Trends, Settings
- Icon imports: BookOpen, TrendingUp, Users from lucide-react
- **Must sync to `/opt/dashboard_build/` before rebuild**

### Auth Session Cookie Issue
- NextAuth CSRF token format: `token|signature` (URL-encoded as `%7C` for `|`)
- Form submit requires CSRF token from `api/auth/csrf` endpoint
- Session cookie: `__Secure-next-auth.session-token`
- Test credentials: `fabio@madhorse.cloud / admin123`

### Nginx Config Location
- Main config: `/etc/nginx/nginx.conf`
- Backup: `/etc/nginx/nginx.conf.bak`
- Reload: `systemctl reload nginx`
- Test: `nginx -t`
- Add proxy blocks via `node` script (python3 has encoding issues on VPS)

### Phase 5→6 Gate
- Compliance Audit: PASS (68 tests, 10 categories, 4 sign-offs)
- Audit report: `projects/P2026-008_ProjectDocuments/documents/Phase5_UAT/AUDIT_REPORT_Phase5_FINAL.md`
- Compliance Auditor subagent confirmed gate cleared

### Research Data Status
- **COO Digest:** `memory/research/DIGEST_latest.md` exists and loads (via MEMORY_RESEARCH_PATH fix)
- **Trends:** Now serving via microserver (8 topics across YouTube/Reddit/Twitter/HN)
- Active agents/sessions: Currently 0 (expected — main session is Boss, subagents idle)

## Project Status
| Project | Phase | Status |
|---------|-------|--------|
| P2026-008 MADHORSE HQ | Phase 6 | Open — Lessons Learned pending |
| P2026-010 AI Arena | Phase 1 | Not started |

## VPS Access
- IP: 76.13.215.13
- Dashboard container: `dashboard` (port 3002)
- Trends microserver: `trends-server` (port 3003)
- Nginx config: `/etc/nginx/nginx.conf`
