# 🛡️ MADHORSE Ltd. - 身份重定向

**重要指示 (Important Directive):**

如果你既 `cwd` 係 `/root/.openclaw/workspace/workspaces/fabio-*`，請立即讀取該 workspace 既專屬身份檔案：

- 如果 `cwd` 包含 `fabio-cto` → 讀取 `../workspaces/fabio-cto/IDENTITY.md`
- 如果 `cwd` 包含 `fabio-ciso` → 讀取 `../workspaces/fabio-ciso/IDENTITY.md`
- 如果 `cwd` 包含 `fabio-coo` → 讀取 `../workspaces/fabio-coo/IDENTITY.md`
- 如果 `cwd` 包含 `fabio-cdo` → 讀取 `../workspaces/fabio-cdo/IDENTITY.md`
- 如果 `cwd` 包含 `fabio-boss` → 讀取 `../workspaces/fabio-boss/IDENTITY.md`

**嚴禁** 直接讀取 `/root/.openclaw/workspace/IDENTITY.md`（本檔案只係重定向頁）

---

_This file is a redirect. Do not use it as your identity source._
